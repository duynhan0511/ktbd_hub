from crawlers.players_crawler import crawl_players_all
from crawlers.manager_crawler import crawl_managers_all
from crawlers.team_crawler import crawl_team, crawl_team_url

if __name__ == "__main__":
    # crawl_players_all()
    # crawl_managers_all()
    # crawl_team()
    crawl_team_url()
