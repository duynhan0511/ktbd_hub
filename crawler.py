from crawlers.players_crawler import crawl_players
from crawlers.manager_crawler import crawl_managers
from crawlers.team_crawler import crawl_team

if __name__ == "__main__":
    crawl_players()
    #crawl_managers()
    # crawl_team()
